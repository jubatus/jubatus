
def options(opt): pass

def configure(conf): pass

def build(bld):
  src = 'rpc_mclient2.cpp rpc_server2.cpp'

  bld.shlib(
    source = src,
    target = 'jubacommon_mprpc',
    use = 'PFICOMMON LIBGLOG ZOOKEEPER_MT LIBEVENT MPIO MSGPACK-RPC MSGPACK jubacommon'
    )

  bld.program(
    features = 'gtest',
    source = 'rpc_client_test.cpp',
    target = 'rpc_client_test',
    includes = '.',
    use = 'PFICOMMON MPIO MSGPACK-RPC MSGPACK jubacommon jubacommon_mprpc',
    )

  bld.install_files('${PREFIX}/include/jubatus/common/mprpc', bld.path.ant_glob('*.hpp'))
